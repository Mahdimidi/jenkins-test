services:
  app: 
    image: malbouz/angpipe:1.1.1
    build:
      context: .
    container_name: angpipe
    ports: 
        - ':4200:4200' # Port mapping
    command: > 
        bash -c "npm install && ng serve --host 0.0.0.0 --port 4200"
